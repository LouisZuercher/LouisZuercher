Intro
 What is SiuozlScript?
  SiuozlScript is not an active language, though can help you in a lot of programs. You can use it to explain any programming language. Let's see the basics!
Basics
 How it works
  Commands
   Well, let'S start by this. If you see SiuozlS 4.1, it's different. But, as long as you use a 2, it should be similar. So, commands for 2.3 are like this:
    Similar to HTML, it uses these simbols: < > . Ok? Now, here's an example: <text |size.24| |clr."black"|
